**ContextGraph**

**复杂多轮 AI 对话与上下文工作空间**

**产品设计文档 · v1.0**

<table style="width:96%;">
<colgroup>
<col style="width: 95%" />
</colgroup>
<thead>
<tr>
<th><p><strong>产品核心命题</strong></p>
<p>用户不是在管理聊天记录，而是在构建一张可执行、可追溯、可版本化的思考网络；AI
对话只是创建和修改这张网络的一种方式。</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

**文档范围**

> — 本文件定义产品理念、核心对象、交互形态、功能边界与多平台体验原则。
>
> —
> 暂不讨论具体前端框架、数据库、模型供应商、部署方式、同步协议或工程架构。
>
> — “ContextGraph”为工作名称，可在品牌阶段更换。

2026 · Product Concept Specification

# 目录与阅读方式

本设计文档从“为什么存在”开始，逐步进入“产品如何组织思考”“用户如何操作上下文”“不同平台如何呈现同一模型”，最后收敛到功能范围与演进路径。

| **01** | 产品定义与边界           |
|--------|--------------------------|
| **02** | 设计原则                 |
| **03** | 核心对象与信息模型       |
| **04** | 核心产品界面             |
| **05** | 对话节点、分支与引用     |
| **06** | Context 管理系统         |
| **07** | AI Context Planner       |
| **08** | Project State 与知识状态 |
| **09** | 合并、冲突与版本演进     |
| **10** | Graph 可视化与导航       |
| **11** | 典型用户流程             |
| **12** | 多平台体验定义           |
| **13** | 模型与 Agent 编排        |
| **14** | 信任、透明与用户控制     |
| **15** | 功能范围与优先级         |
| **16** | 产品演进路线             |
| **17** | 术语表                   |

# 01 产品定义与边界

## 1.1 一句话定义

<table style="width:96%;">
<colgroup>
<col style="width: 95%" />
</colgroup>
<thead>
<tr>
<th><p><strong>产品定义</strong></p>
<p>ContextGraph 是一个面向复杂、多轮、长期 AI
工作流的上下文工作空间。它把线性聊天转化为可组合的对话网络，让用户能够从任意内容发起支线、显式选择模型所读取的上下文、沉淀稳定的项目状态，并通过
AI 推荐减少上下文管理负担。</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

## 1.2 产品要解决的问题

现有主流聊天产品普遍以不断增长的线性日志作为核心组织方式。线性结构适合短问答，却不适合真实的复杂思考：用户会临时解释术语、验证假设、比较方案、追问局部内容、推翻旧结论，并在数天或数月后回到同一主题。所有历史都沿单一时间轴堆积，会产生上下文污染、失效结论残留、检索困难、Token
浪费，以及“模型到底看了什么”不可见等问题。

ContextGraph
不把这些问题视为单纯的聊天历史管理，而把它们定义为“上下文的组织、选择、流动、版本化与治理”问题。产品的基本单位因此从
Conversation 升级为 Project、Graph、Node、State 与 Context Manifest。

## 1.3 目标用户

第一阶段应优先服务高度依赖复杂多轮工作流的人群，包括研究人员、软件工程师、产品经理、咨询顾问、律师、分析师、复杂内容创作者，以及任何需要长期与
AI
共建项目的人。普通用户仍可使用，但产品不应为了“大众聊天”牺牲专业上下文能力。

## 1.4 明确非目标

产品不是单纯的思维导图工具，也不是把每条消息画成框的“树状聊天”；不是普通知识库
RAG
的可视化包装；不是要求用户每次发送前手工勾选大量节点的专家工具；也不是一开始就以无人值守
Agent 自动化为主要卖点。核心目标始终是：让复杂 AI
对话的上下文结构变得可见、可控、可复用。

<img src="media/image1.png" style="width:6.45669in;height:3.06693in" />

图 1 · 产品核心抽象层次

# 02 设计原则

### 01 对话是探索过程，项目状态才是资产

聊天记录可以冗长、反复甚至互相矛盾；产品应允许探索保持原貌，同时将经过确认的事实、约束、决定与产出沉淀为稳定状态。

### 02 上下文必须显式

用户应始终能够知道一次 AI
回复读取了什么、为什么读取、哪些内容被排除，以及上下文大致占用多少预算。

### 03 AI 推荐，人类拥有最终控制权

系统可以自动检索与推荐，但 Recommended Context 与 Active Context
必须在概念上分离。默认体验可以自动化，高级体验必须可审计、可覆盖。

### 04 图优先于树

真实思考不仅会分叉，也会汇聚、交叉引用、产生依赖和冲突。产品数据模型从一开始就应允许
DAG 关系，并为更一般的图关系保留空间。

### 05 默认简单，复杂度渐进暴露

初次使用应接近普通 Chat；只有当用户需要控制时，才展开 Context
Inspector、Graph View、Project State 等能力。

### 06 来源可追溯

任何重要结论都应可以回到其来源讨论、文件段落或用户输入。重要状态的修改应能看见前后版本和受影响的下游节点。

### 07 语义地位高于文本相似

“已确认事实”“约束”“假设”“反例”“过期结论”虽然都是文本，但对模型具有不同意义。产品必须表达这些角色。

### 08 跨平台共享同一认知模型

桌面、网页、移动端不应形成三套产品。差异只应体现在信息密度、交互方式和同时可见区域，而不是核心对象与逻辑。

# 03 核心对象与信息模型

产品需要把“用户看到的节点”“底层聊天记录”“模型实际读取的上下文”解耦。用户可以把一个长期讨论理解为一个话题节点，但该节点内部仍由多条原始输入、回复、文件引用与工具结果组成；模型调用时则通过
Context Manifest 选择其中真正有效的部分。

| **Workspace** | 最高层工作空间，可容纳多个 Project，并保存用户偏好、模板、Agent Profile 等共享资源。 |
|----|----|
| **Project** | 一个长期主题或任务的完整容器。包含 Conversation Graph、Project State、文件、产出与配置。 |
| **Discussion Node** | 用户可见的主要工作单元，代表一个话题、问题、假设、决策或任务。 |
| **Discussion Segment** | 节点内部的一段连续对话。用于把大量消息聚合为可管理的语义单元。 |
| **Message / Event** | 最细粒度的原始记录：用户输入、AI 回复、工具结果、文件引用、状态变更等。 |
| **Anchor** | 分支来源锚点。可精确指向某条消息、某个段落、任意文字范围、文件片段、图片区域或已有节点。 |
| **Edge** | 节点之间的关系。至少包括派生、引用、依赖、合并、反驳、补充、替代等语义。 |
| **Project State** | 项目当前有效的半结构化知识状态。保存事实、约束、决策、假设、开放问题、待办与产出。 |
| **Context Manifest** | 一次模型调用的上下文清单，记录当前话题、引用节点、文件片段、状态项、角色、排除项与预算。 |
| **Artifact** | 用户或 AI 产生的文件、代码、报告、图片、表格等成果，可被节点引用并进入 Project State。 |
| **Agent / Model Profile** | 节点或调用层面的模型、行为、工具与指令配置。 |

## 3.1 节点不是消息

Graph View 不应默认把每条 User/Assistant Message
都显示为一个节点。一个节点是“有意义的认知单元”，内部可以包含十几轮乃至更多消息。只有在局部展开或审计时，用户才进入消息级视图。这样图的复杂度随主题数量增长，而不是随消息数量爆炸。

## 3.2 Anchor 支持任意粒度

用户可以从当前回答中的一个词、一句话、一个段落、代码片段、文件引用或整段历史发起新讨论。新节点应永久保留它从哪里产生，因此支线不只是“从某条消息
fork”，而是“从具体语义对象展开”。

# 04 核心产品界面

产品的主要体验不应只有一张复杂大图，而应由几个互补视图组成。用户绝大多数时间仍停留在
Chat View；Context 与 Graph 能力在需要时逐渐展开。

| **Chat View** | 主工作界面。完成对话、选中文字发起支线、引用节点、保存结论、查看当前 Context 摘要。 |
|----|----|
| **Context Inspector** | 显示当前 Active Context、Recommended Context、固定项、排除项、角色、来源与 Token 预算。允许一键加入、移除、锁定或改角色。 |
| **Graph View** | 展示整个主题的节点关系、分支、依赖、冲突与合并。支持拖拽组合上下文、聚类、筛选、语义缩放。 |
| **Project State View** | 展示当前有效事实、约束、决策、假设、开放问题、任务和产出，并能回溯来源。 |
| **Node Detail** | 聚焦单节点，查看节点摘要、完整消息、引用来源、状态、关联边、模型配置与历史版本。 |
| **Context Tray** | 临时收集节点、段落、文件和状态项。用户可以像把材料放入“托盘”一样组合上下文，然后基于托盘创建新讨论。 |
| **Search / Command** | 跨节点、消息、文件、状态项检索；同时承担快速跳转、创建分支、合并、固定上下文等高频命令入口。 |

<table style="width:96%;">
<colgroup>
<col style="width: 95%" />
</colgroup>
<thead>
<tr>
<th><p><strong>默认体验</strong></p>
<p>用户打开一个 Project
后，看到的仍然应该是清晰的聊天界面，而不是被迫面对复杂网络图。Graph
是强能力，不是默认负担。</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

# 05 对话节点、分支与引用

## 5.1 从任意内容发起讨论

用户选中任意词句、段落、代码块、图片或文件片段后，可直接选择“在新支线中讨论”。系统创建一个临时
Branch，并自动继承必要来源信息。用户无需提前理解图结构。

## 5.2 临时支线与正式节点

为了避免图被大量一次性追问污染，新支线默认可以是临时态。讨论完成后，用户可以选择保留为正式节点、提取结论后合并、仅保留引用，或直接丢弃。只有被保留的认知单元才进入长期
Graph。

## 5.3 节点生命周期

| **Draft**      | 刚创建或仍在探索，内容可能改变。                 |
|----------------|--------------------------------------------------|
| **Active**     | 当前有效、可被推荐和默认引用。                   |
| **Resolved**   | 问题已获得稳定结论，但仍可被引用。               |
| **Stale**      | 依赖的上游事实或约束改变，结论可能需要重新验证。 |
| **Superseded** | 已被新节点或新结论替代，仅用于历史回溯。         |
| **Archived**   | 用户主动归档，不参与默认上下文推荐。             |

## 5.4 边的语义

边不应只是“父子关系”。产品至少要能表达 Derived From、References、Depends
On、Supports、Contradicts、Supersedes、Merged Into、Related To
等关系。普通用户可只看到自然语言图标，高级用户可以显式编辑语义。

# 06 Context 管理系统

Context 是本产品真正的核心管理对象。用户所看到的聊天、图、Project
State，最终都服务于一个问题：当前这次模型调用应该读取哪些信息，以及这些信息以什么身份进入模型。

## 6.1 Context 的组成层

| **Current** | 当前用户输入、所在节点及必要的局部对话。 |
|----|----|
| **Pinned** | 用户固定加入的长期上下文，在本节点或项目内持续有效。 |
| **Recommended** | AI 认为相关、但尚未成为有效输入的候选上下文。 |
| **Auto-Retrieved** | 在 Auto 模式下由规则或 AI 自动加入的相关内容，必须可见并可追溯。 |
| **Project State** | 项目中当前有效的事实、约束、决策等状态项。 |
| **Files & Artifacts** | 指定文件、文件局部、代码、图像或其他产出。 |
| **Excluded** | 用户明确禁止进入当前模型调用的内容。 |

## 6.2 Context 角色

加入上下文时，内容不应只有“包含/不包含”两个状态，还应有语义角色。首版建议支持
Fact、Constraint、Decision、Reference、Hypothesis、Counterargument、Instruction、Deprecated
Reference。模型与 UI 都应明确区分这些角色。

## 6.3 三种控制模式

| **Auto** | 系统自动选择相关上下文。适合普通用户，但所有自动加入项仍需在回复后可检查。 |
|----|----|
| **Assisted** | AI 提供 Recommended Context，用户快速确认或调整。建议作为产品默认专业模式。 |
| **Strict** | 仅允许读取用户明确指定的上下文；AI 不得私自加入项目历史。适合高可控、高审计场景。 |

## 6.4 Context Budget

输入框附近应持续显示当前上下文规模，例如“Context
42.8K”。当候选信息超过模型预算时，系统不应简单截断，而应向用户呈现压缩、摘要或优先级策略。用户需要知道哪些内容因为预算原因没有进入。

<img src="media/image2.png" style="width:6.41732in;height:3.60974in" />

图 2 · Context 从候选到模型调用的生命周期

# 07 AI Context Planner

当项目拥有几十或几百个节点后，纯手工选择上下文会成为新的负担。因此 AI
Context Planner
不是附加功能，而应成为系统规模化后的核心体验层。它负责在“用户拥有最终控制权”的前提下帮助发现、解释、压缩和组合相关上下文。

## 7.1 推荐而非黑箱注入

Recommended Context 与 Active Context 必须清晰分开。系统可以提示“发现 4
个相关节点”，但在 Assisted
模式下，只有用户确认的内容才进入本次调用。Auto
模式也必须留下完整记录，用户可在回答后看到“本次自动加入了哪些内容”。

## 7.2 推荐依据

Planner 的产品逻辑应综合语义相关性、图距离、依赖关系、Project State
权重、状态有效性、时间与版本、用户历史选择、冲突风险，以及上下文预算。产品层面不限定具体算法，但不能把它定义成简单的向量相似度搜索。

## 7.3 Why this context?

每个推荐项都应提供一句可理解的解释，例如：“当前问题提到
pytest-html，而该节点记录了此前环境中的 pytest-html
版本冲突。”同样，用户也可以询问为什么某节点没有被推荐，系统应说明是主题无关、已经过期、属于不同环境，还是预算不足。

## 7.4 预算优化与压缩

当潜在相关内容远超上下文预算时，Planner
应优先寻找“信息覆盖率最高的组合”，并允许对低优先内容使用节点摘要、状态项或局部片段代替完整聊天。压缩行为必须被标注，用户可以选择展开原文。

## 7.5 推荐反馈

用户对推荐的接受、删除、固定、改角色行为，应作为产品个性化的重要信号。系统可逐渐学习某个用户在特定项目中的上下文习惯，但不得因此隐藏或削弱用户的显式控制。

# 08 Project State 与知识状态

Conversation Graph 记录的是“我们如何得到结论”，Project State
保存的是“现在什么是有效的”。二者必须同时存在。否则长期项目仍需要模型反复从大量历史聊天中重新推断当前事实。

## 8.1 State 类型

| **Fact**          | 已经确认的客观信息。           |
|-------------------|--------------------------------|
| **Constraint**    | 方案必须满足或不得违反的条件。 |
| **Decision**      | 已经做出的选择及其理由。       |
| **Hypothesis**    | 当前正在验证、尚未确认的推断。 |
| **Open Question** | 仍待解决的问题。               |
| **Task**          | 需要执行或跟进的工作。         |
| **Reference**     | 项目持续使用的外部或内部资料。 |
| **Artifact**      | 已经产生并被认可的成果。       |

## 8.2 来源与版本

每一个 State Item 都应保留
provenance：来源节点、来源段落或文件、确认时间、确认者、当前状态和替代关系。用户点击一个约束，应能直接回到当时为什么做出这个约束的讨论。

## 8.3 更新与失效传播

当一个上游事实、约束或决策被修改时，系统应检查依赖它的下游节点，并标记为可能
Stale。产品不需要替用户自动推翻结论，但要清楚提示“哪些已有结论可能受影响”。这使长期项目具备类似版本控制与依赖管理的稳定性。

<table style="width:96%;">
<colgroup>
<col style="width: 95%" />
</colgroup>
<thead>
<tr>
<th><p><strong>关键区别</strong></p>
<p>Summary 是对过去聊天的压缩；Project State
是对“当前有效知识”的治理。二者不能混为一谈。</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

# 09 合并、冲突与版本演进

## 9.1 Selective Merge

支线讨论结束后，不应只有“关闭”或“整段合并”两种结果。用户需要选择合并粒度：完整讨论、自动摘要、指定结论、若干状态项、结构化变量、Artifact，或仅建立引用关系。

## 9.2 冲突检测

当两个被同时引用的节点包含相互矛盾的有效结论，系统应在 Context Inspector
和 Graph View
中显式标记冲突。用户可以指定其中一个为当前有效、将其中一个降级为
Hypothesis，或创建一个新的 Synthesis Node 解决冲突。

## 9.3 Supersede 而非删除历史

旧结论被新结论替代时，默认不删除原内容，而是建立 Supersedes
关系。这样产品既能保持当前状态干净，又保留完整思考历史和可审计性。

## 9.4 Recompute / Revisit

当上游条件变化导致节点 Stale
时，用户可以选择“重新讨论此节点”，系统自动用最新依赖创建一个新版本；旧版本仍保留，二者形成清晰的版本演进关系。

# 10 Graph 可视化与导航

完整对话网络必须避免“意大利面图”。Graph View
的目标不是一次展示一切，而是帮助用户在不同尺度理解项目结构。

## 10.1 Semantic Zoom

最远层级显示 Project 与 Topic Cluster；放大后显示 Discussion
Node；继续进入才显示节点内部 Segment；只有在审计或局部分析时才展开单条
Message。图的可视复杂度因此随语义层级控制。

## 10.2 自动聚类与布局

系统可根据用户建立的边、语义主题和时间关系形成 Topic
Cluster。自动布局只负责提高可读性，不应改变用户建立的逻辑关系。用户可以固定节点位置、建立区域、命名主题簇。

## 10.3 过滤与图层

用户应能按状态、时间、节点类型、关系类型、模型、是否进入 Project
State、是否 Stale
等维度过滤。还可以切换“依赖视图”“时间视图”“冲突视图”“当前 Context
视图”等图层。

## 10.4 从图直接操作 Context

Graph 不只是展示。用户可以多选节点拖入 Context
Tray，或直接选择“基于所选节点开始新讨论”；也可以把当前节点与另一个节点建立引用、依赖、反驳或合并关系。

# 11 典型用户流程

## 11.1 局部追问：从一句话开支线

用户在主线回答中选中“GCRO-DR”，点击“在支线中询问”。临时 Branch
打开，只继承必要来源。用户讨论十轮后选择“保存结论”，系统把一条简明
Reference 合并回主项目，支线完整历史仍可追溯，但不自动污染主线 Context。

## 11.2 复杂组合：跨节点创建新讨论

用户依次把“Runner 架构”“SSH 排障”“Python 环境隔离”和一个测试说明文件拖入
Context
Tray，然后创建“最终测试部署方案”节点。新节点建立对前三个节点和文件的依赖边，Context
Manifest 明确记录本次输入来源。

## 11.3 状态变化：旧结论失效

用户把 Project State 中“禁止使用 Docker”的约束改为“允许
Docker”。系统提示有若干节点依赖该约束，并将其标为
Stale。用户可逐个忽略、确认仍有效，或触发基于新约束的重新讨论。

## 11.4 AI 推荐：用户不主动管理图

普通模式下，用户只在 Chat 中提问。系统提示“发现 3
个相关上下文”，默认以轻量卡片展示。用户一键接受或忽略，无需进入
Graph。只有需要精细控制时才打开 Context Inspector。

## 11.5 多模型比较

用户从同一 Context Manifest
创建三个并行节点，分别由不同模型处理，再建立一个 Synthesis Node
综合结果。Graph
自然表达“同源分叉—并行推理—重新汇聚”，而不需要特殊的独立产品模式。

# 12 多平台体验定义

桌面端、网页端和移动端共享同一套对象、状态和行为语义。平台差异只体现在“同时展示多少信息”与“如何触达复杂功能”。任何平台产生的节点、Context
Manifest、Project State 和 Graph 关系都应在其他平台无损继续。

<img src="media/image3.png" style="width:6.45669in;height:3.43012in" />

图 3 · 多平台共享核心模型，采用不同交互密度

## 12.1 桌面端

桌面端承担最高信息密度。适合三栏或可拆分多窗格：左侧 Project/Topic
Navigator，中间 Chat，右侧 Context Inspector；Graph
可以全屏、分屏或独立窗口出现。应支持高效率键鼠操作、拖拽、快捷键与大规模图浏览。

## 12.2 网页端

网页端是完整主工作台，而不是功能缩水版。根据视口采用双栏或三栏，Graph 与
Project State
可作为主页面切换。重点保持无安装可访问性、跨设备连续性以及足够完整的专业能力。

## 12.3 移动端 App

移动端强调单任务聚焦：默认只显示 Chat；Context Inspector
以底部抽屉或全屏页呈现；Graph
进入独立全屏模式，通过缩放、搜索、聚焦当前节点降低复杂度。移动端不应强求同时展示完整图和聊天，而应确保“查看上下文、接受推荐、从选中文字开支线、保存
State”这些关键行为足够顺手。

## 12.4 跨平台一致原则

同一动作在各端必须保持相同语义。例如“Pin
Context”无论通过桌面拖拽、网页菜单还是移动端长按完成，结果都应是同一种
Pinned Context 状态。平台可以改变手势，不能改变概念。

# 13 模型与 Agent 编排

在成熟形态下，每个节点可以拥有独立的 Model / Agent
Profile，但这一能力应建立在 Context Graph
之上，而不是反过来把产品变成复杂工作流编辑器。

## 13.1 节点级模型选择

用户可为不同节点选择不同模型或预设：代码分析、长文档阅读、批判性审查、总结、创意探索等。节点保留使用的
Profile，使结果可追溯。

## 13.2 Compare 与 Synthesize

同一 Context 可并行派生多个模型节点，再通过 Synthesis Node 汇总。Graph
天然表达这种模型编排，因此无需让用户学习传统流程图式工作流。

## 13.3 从 Conversation Node 到 Agent Node

后续可以允许部分节点从人工对话转变为可重复执行的 Agent
Node，例如“当上游架构状态改变时重新检查此结论”。但首要原则仍是保持节点的来源、依赖、Context
和输出可见。

# 14 信任、透明与用户控制

## 14.1 模型读取范围可见

每条 AI 回复应允许用户查看其 Context
Manifest。至少能够回答：使用了哪个模型、所在节点、读取了哪些节点或文件、哪些
Project State 项被作为约束、哪些内容经过摘要、哪些候选被排除。

## 14.2 AI 不应伪装确定性

系统必须区分“AI 推荐”“用户确认”“项目当前有效状态”。AI
认为某条信息重要，不等于它已经成为 Project State；AI
发现冲突，也不等于它有权替用户选择正确版本。

## 14.3 可逆性

创建节点、合并、改变关系、更新 State、标记 Superseded
等核心行为都应具有清晰的撤销或版本回溯方式。用户需要敢于探索，而不担心破坏长期项目结构。

## 14.4 隐私与项目边界

不同 Project
默认应被视为独立上下文边界。跨项目引用必须显式发生。后续若加入团队协作，节点、State
与 Artifact 的共享范围也应成为可见属性，而不能在后台模糊流动。

# 15 功能范围与优先级

以下优先级用于界定产品设计阶段的“首要体验”与“长期能力”，并不绑定具体开发周期。P0
表示没有它就无法验证核心产品命题；P1 表示形成完整专业体验；P2
表示在核心模型成立后扩展产品边界。

| **功能域** | **功能定义** | **用户价值** | **阶段** |
|----|----|----|----|
| **Project / Node 基础** | Project 管理；节点内多轮对话；节点摘要与状态。 | 形成非线性对话的基本容器 | P0 |
| **任意文本分支** | 从词句、段落、代码块等 Anchor 创建临时/正式支线。 | 让分支贴近真实思考行为 | P0 |
| **Graph View** | 节点、关系、聚类、聚焦与基础语义缩放。 | 把长期讨论变成可导航结构 | P0 |
| **Context Inspector** | 查看、加入、排除、固定节点与文件。 | 把上下文从黑箱变为显式对象 | P0 |
| **Context Manifest** | 为每次模型调用保存可审计上下文清单。 | 建立可解释性与可复现性 | P0 |
| **Selective Merge** | 支线按完整讨论、摘要、结论或引用方式回流。 | 隔离探索与有效知识 | P0 |
| **Project State** | 事实、约束、决策、假设、开放问题与来源。 | 让长期项目拥有稳定知识状态 | P1 |
| **AI Context 推荐** | 推荐候选上下文，解释推荐原因，用户确认。 | 降低图规模增长后的操作成本 | P1 |
| **Context 角色** | Fact / Constraint / Hypothesis / Reference 等语义。 | 提高模型对内容地位的理解 | P1 |
| **冲突与 Stale** | 冲突提示、Supersede、依赖变化后的失效标记。 | 治理长期项目的一致性 | P1 |
| **Context Tray** | 跨节点、文件和段落拖入托盘后创建新讨论。 | 让组合上下文成为直接操作 | P1 |
| **高级 Graph** | 依赖/时间/冲突图层，过滤，固定布局，主题区域。 | 支持超大型项目导航 | P1 |
| **多模型 Compare** | 同 Context 并行调用不同模型并综合。 | 扩展为模型编排工作区 | P2 |
| **Agent Node** | 节点可重复执行、根据上游变化重新检查。 | 从思考网络演进为可执行网络 | P2 |
| **团队协作** | 共享节点、状态、评论、权限与共同 Project State。 | 扩展到组织级知识工作 | P2 |

# 16 产品演进路线

## 阶段 A：Conversation Graph

首先证明用户确实愿意用非线性结构管理复杂对话。核心体验是：从任意文本开支线、节点级讨论、Graph
View、Context Inspector、Selective Merge 与 Context
Manifest。此阶段的产品仍然可以看起来像“更高级的
Chat”，但底层模型已经改变。

## 阶段 B：Context IDE

加入 Project State、Context Role、AI Context Planner、Context
Tray、冲突与 Stale
管理。此时产品的核心价值从“分支聊天”升级为“可视、可控、可组合的上下文工作空间”。

## 阶段 C：Knowledge & Model Orchestration

增加多模型并行、Synthesis Node、更强的项目知识治理、模板与领域化
Profile。Graph 开始同时承担思考结构与模型协作结构。

## 阶段 D：Executable Thought Network

在不破坏人工控制的前提下，让部分节点具备重复执行、依赖触发、检查与更新能力；随后再考虑团队协作、共享项目状态与组织知识流。最终形态是“可执行、可追溯、可版本化的思考网络”。

<table style="width:96%;">
<colgroup>
<col style="width: 95%" />
</colgroup>
<thead>
<tr>
<th><p><strong>长期产品定位</strong></p>
<p>Chat → Conversation Graph → Context Graph → Knowledge State → Agent
Graph。演进顺序非常重要：先解决人类如何组织上下文，再逐步增加自动化，而不是一开始就把产品做成复杂
Agent 工作流平台。</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

# 17 术语表

| **ContextGraph** | 本文档中的产品工作名。 |
|----|----|
| **Conversation Graph** | 由讨论节点和语义边组成的思考过程网络。 |
| **Node** | 可被用户理解、引用、组合和管理的语义讨论单元。 |
| **Anchor** | 创建分支时所指向的精确来源位置。 |
| **Context** | 模型在某次调用中可读取的信息集合。 |
| **Recommended Context** | AI 建议加入、但未必已经生效的候选内容。 |
| **Active Context** | 本次调用实际会读取的有效上下文。 |
| **Context Manifest** | 冻结并记录本次 Active Context 的清单。 |
| **Context Inspector** | 查看和编辑 Context 的主要界面。 |
| **Context Tray** | 临时组合多个节点、段落、文件与状态项的容器。 |
| **Project State** | 项目当前有效、可追溯的半结构化知识状态。 |
| **Selective Merge** | 从支线按不同粒度将有效内容回流到主项目。 |
| **Stale** | 由于上游依赖变化而可能失效的节点状态。 |
| **Superseded** | 已经被新版本或新结论替代但仍保留历史的状态。 |
| **Semantic Zoom** | 根据缩放层级显示 Project、Topic、Node、Segment、Message 等不同语义粒度。 |
| **Context Planner** | AI 驱动的上下文候选发现、推荐、解释、预算与压缩能力。 |

**END OF PRODUCT DESIGN DOCUMENT**

**核心目标：让 AI
上下文从隐藏机制变成用户可以观察、组织、组合、版本化与复用的第一类产品对象。**
